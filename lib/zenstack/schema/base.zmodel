import "./auth"
import "./courses"

datasource db {
  provider = 'sqlite'
  url = 'file:../../../data/dev.db'
}

plugin policy {
  provider = "@zenstackhq/plugin-policy"
}

/// Shape of the `auth()` function
type Auth {
  userId String  @id
  role   String?

  @@auth
}
